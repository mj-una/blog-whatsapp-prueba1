---
import Logs from "./Logs.astro";

import base from "../../utils/exportBase";
import seleccionarArchivo from "../../utils/seleccionarArchivo";
import seleccionarArchivoLog from "../../utils/seleccionarArchivoLog";

const { id, nombre } = Astro.props;
const rutaAvatar = seleccionarArchivo(base + "/avatar", id, "_default", "webp");
const logs = seleccionarArchivoLog(base + "/avatar", id, "_default", "webp");

---
 
<!-- --ACS-- -->
<div class="contenedor-caja-nombre">
  <div class="caja-nombre">
    <a class="boton-atras" href={`${base}`}>ᗕ</a>
    <img class="avatar"
      src={rutaAvatar}
      alt="Imagen de perfil"
    />
    <h3 class="nombre">
      {nombre}
    </h3>
  </div>
  <Logs client:load logs={logs} />
</div>

<!-- --CCS-- -->
<style>
  .contenedor-caja-nombre {
    width: 100%;
    height: 100%;
    padding: 10px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .caja-nombre {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: left;
    align-items: center;
  }

  .boton-atras {
    margin-left: 4px;
    margin-right: 0px;
    font-size: 1.2rem;
    text-decoration: none;
    color: var(--cl-blanco-sld-texto);
  }

  .avatar {
    width: 40px;
    aspect-ratio: 1 / 1;
    margin: 10px;
    margin-right: 12px;
    border-radius: 50%;
  }

  .nombre {
    font-size: 1.1rem;
    font-weight: var(--fw-semibold);
    color: var(--cl-blanco-sld-texto);
  }
</style>